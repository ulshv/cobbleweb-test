import React from 'react';
import styled from '@emotion/styled';

const Title = styled.div`
  margin: 8px 0 16px;
  font-size: 20px;
  font-weight: bold;
  color: #333;
  text-align: center;
`;

export default Title;
